{
  "name": "sonali-coldrinks-bakery",
  "version": "1.0.0",
  "description": "Sonali Coldrinks & Bakery — Full Stack App",
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"yellow,cyan\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "create-admin": "node backend/create_admin.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
